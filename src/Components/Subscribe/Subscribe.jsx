import styles from "./Subscribe.module.scss";

const Subscribe = () => {
  return (
    <section className={`${styles.subscribe}`}>
      <div className={`${styles.subscribe__inner}`}></div>
    </section>
  );
};

export default Subscribe;
